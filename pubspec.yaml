name: text_converter
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

flutter:
  uses-material-design: true

  assets:
    - assets/images/1.jpg
    - assets/images/2.jpg

environment:
  sdk: '>=3.2.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  image_picker: ^0.8.7+4 # برای انتخاب فایل
  qr_flutter: ^4.0.0 # برای ساخت QR Code
  path_provider: ^2.0.11 # برای ذخیره فایل‌ها
  permission_handler: ^10.2.0 # برای دسترسی به فایل‌ها

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter_icons:
  android: true
  ios: true
  image: "assets/icons/app_icon.png"
